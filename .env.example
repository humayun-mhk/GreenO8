# Backend base URL supplied by the Greenova8 backend team.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Endpoint must return ONLY verified/listed projects. No mock fallback is used.
NEXT_PUBLIC_PROJECTS_ENDPOINT=/api/projects?status=verified&listed=true
